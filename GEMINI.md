# CRNN OCR 프로젝트 현황 (YGC)

## 1. 프로젝트 목표

- 차량 번호판 이미지에서 텍스트를 정확히 인식하기 위한 OCR(Optical Character Recognition) 파이프라인 구축
- **YOLOv5**, **CRAFT**, **CRNN+CTC**를 결합한 3단계 구조의 시스템 설계:
  1. **YOLOv5**: 번호판 탐지
  2. **CRAFT**: 번호판 내부의 문자 또는 단어 영역 분리
  3. **CRNN+CTC**: 분리된 문자를 인식하여 전체 번호판 문자열 복원

---

## 2. 현재까지의 진행 상황

### YOLOv5 (번호판 영역 탐지)
- `yoloBestModel.pt` 학습 완료
- AIHub 차량번호판 데이터셋 기반으로 학습
- 추론 시 차량 이미지에서 번호판 박스를 정확히 탐지함

### CRNN+CTC (문자 인식)
- `ocr/train_v1_4_2.py`를 통해 `ocrBestModel_142.pth` 학습 완료 (실제 데이터 기반)
- 커스텀 charset(한글 + 숫자 + 영문 조합) 사용
- 학습 결과는 `results/ocr_results/`에 저장

### 가상 데이터셋 구축 (2025-07-16)
- **목표**: 모델의 문자 인식 능력 강화를 위해 대규모 학습 데이터셋 확보
- `ocr/scripts/create_virtual_data.py`: 한글, 자모, 영문, 숫자를 포함하는 포괄적인 문자셋으로 가상 문자 이미지 및 라벨 생성
- `ocr/scripts/create_virtual_data_val.py`: 모델 검증을 위한 별도의 가상 데이터셋 생성

### 가상 데이터 학습 파이프라인 구축 (2025-07-16)
- `ocr/train_v1_5_1.py`: 가상 데이터셋(`virtual_data`, `virtual_val_data`)을 사용하여 CRNN 모델을 학습하는 스크립트 신규 작성
- 확장된 `VIRTUAL_CHARSET`을 `LabelEncoder`에 적용하여 모든 가상 문자를 학습 가능하도록 구성

### 프로젝트 구조
- 학습 및 추론 스크립트 구조화 완료 (`ocr/model/crnn.py`, `ocr/utils/dataset.py`, `ocr/utils/label_encoder.py` 등)

---

## 3. 기존 시도 및 발생한 문제점

### CRAFT 기반 문자 분리의 한계
- **문제**: CRAFT는 단어 단위로 영역을 분리하므로, "12가3456"과 같은 번호판 문자열을 개별 문자로 정확히 잘라내기 어려움.
- **결과**: 부정확한 문자 분리는 CRNN 모델의 학습 오류 및 성능 저하로 이어짐. 이 문제를 해결하기 위해 가상 데이터셋 구축으로 방향 전환.

---

## 4. 현재 진행 중인 작업

- **가상 데이터셋 기반 모델 학습**: `train_v1_5_1.py` 스크립트를 통해 대규모 가상 데이터로 CRNN 모델의 기초 문자 인식 성능을 강화하는 작업을 진행 중.
- **목표**: 실제 번호판 이미지의 복잡한 전처리(CRAFT 분리 등)에 의존하기 전에, 모델 자체가 다양한 문자를 정확히 인지하는 능력을 우선적으로 확보.

---

## 5. 향후 계획 및 개선 포인트

- [ ] **(진행중)** `train_v1_5_1.py` 실행하여 가상 데이터셋으로 CRNN 모델 학습 (`ocrBestModel_151.pth` 생성 목표)
- [ ] 학습된 모델(`ocrBestModel_151.pth`)을 사용하여 실제 번호판 이미지(YOLO-crop)에 대한 OCR 성능 테스트
- [ ] CRAFT의 단어 박스를 2개 그룹으로 묶는 전처리(`preprocess_craft_merge_group.py`) 로직 재검토 및 구현
- [ ] 번호판 구조 패턴 기반 후처리 로직 (`정규표현식 검증 + 자가 수정 알고리즘`)
- [ ] 다양한 번호판 포맷(자주/새로/세로형 등)에 대응 가능한 서브 모델 실험

## 7. 작업내용 및 리포트
- 작업 진행 내용에 대한 자료는 `report`라는 폴더안에 날짜별로 저장
- 내가 작업진행을 하면서 오늘한 작업에 대해서 정리해달라고 하면 `report` 폴더에 있는 자료의 형식에 맞춰서 작업내용을 정리